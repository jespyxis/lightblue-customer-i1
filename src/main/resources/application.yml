server:
  context-path: /micro
  port: 8080

spring:
  cloudant:
    username: ${vcap.services.Cloudant-i1.credentials.username}
    password: ${vcap.services.Cloudant-i1.credentials.password}
    host: ${vcap.services.Cloudant-i1.credentials.host}
    protocol: https
    port: 443
    database: customers